"""
Analysis app — services (business logic layer).

Phase 5 will add:
    run_comparison(document_ids, question) -> ComparisonResponse
    run_contradiction_detection(document_ids) -> ContradictionResponse
    run_multi_hop_query(question, document_ids) -> AgentResponse
"""
